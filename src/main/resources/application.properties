spring.application.name=TuTrade
spring.jpa.database=postgresql
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost/bdTuTrade}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:prime}

jwt.secret=${JWT_SECRET:TuTrade#Grupo4@UPC-2024$xK9mP2qLrB7nZ}
jwt.expiration=86400000

# HU08 - Impacto Ambiental (Climatiq). Key hardcodeada como default para la demo/presentación
# (se puede sobreescribir con la var de entorno CLIMATIQ_API_KEY). OJO: para algo real, rotarla
# y sacarla del repo. activity_id verificados en ClimatiqClient (EPA/US, spend-based USD).
climatiq.api.key=${CLIMATIQ_API_KEY:X758WFHYA97CF8MEQ0ADHA1EE0}
climatiq.data.version=${CLIMATIQ_DATA_VERSION:^6}

server.port=${PORT:8082}

# Chatbot landing page (Groq). Key hardcodeada como default para la demo local
# (se puede sobreescribir con la var de entorno GROQ_API_KEY). OJO: para algo real,
# rotarla y sacarla del repo. Sin key -> /chatbot responde 503, no rompe el arranque.
groq.api.key=${GROQ_API_KEY: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}
groq.model=${GROQ_MODEL:llama-3.3-70b-versatile}

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs